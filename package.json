{
  "name": "zx-kit-player",
  "private": true,
  "type": "module",
  "scripts": {
    "songs:generate": "node scripts/generate-song-library.mjs",
    "build": "npm run songs:generate",
    "archive": "npm run build && node scripts/archive-project.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "vite"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "vite": "^8.1.3",
    "zip-lib": "1.4.0"
  }
}
